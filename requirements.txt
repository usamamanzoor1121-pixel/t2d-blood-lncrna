# Pinned to the exact versions used to generate the committed results
# (Python 3.10.12, WSL Ubuntu venv — see README "Reproducing this analysis").
# Only direct dependencies are pinned with a rationale; the rest are the
# transitive versions actually resolved in that environment, included for
# byte-for-byte reproducibility.

# --- direct dependencies (imported by 00-05_*.py) ---
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
statsmodels==0.15.0
matplotlib==3.10.9
requests==2.34.2

# --- transitive (pinned for full reproducibility) ---
certifi==2026.7.22
charset-normalizer==3.5.1
contourpy==1.3.2
cycler==0.12.1
fonttools==4.65.0
idna==3.20
kiwisolver==1.5.1
packaging==26.3
patsy==1.0.3
pillow==12.3.0
pyparsing==3.3.3
python-dateutil==2.9.0.post0
pytz==2026.4
six==1.17.0
urllib3==2.8.0
